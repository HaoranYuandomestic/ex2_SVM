# Support Vector Machine

This respority completes the task of the course **Machine Learning**, which requires us to achieve multi-classification via SVM model.

# About the report
You just need to open `data/main.pdf`, and you can see the report. To see more, please enter `data/main.tex` to vie the latex code.

## Running Procedure
First install the required packages
``` bash
pip install -r requirements.txt
```
Then run the code
```bash
python main.py
```
